<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="website_search_box_inherit" inherit_id="website.website_search_box">
        <xpath expr="//input[@name='search']" position="attributes">
            <attribute name="t-att-placeholder">
                "Buscar..." if not (website and website.search_placeholder) else ''
            </attribute>
            <attribute name="class" add="tp-search-input"/>
        </xpath>

        <xpath expr="//input[@name='search']" position="after">
            <div class="tp-search-wrapper">
                <span t-if="website and website.search_placeholder"
                    t-att-class="'tp-fake-placeholder ' 
                                + (website.search_placeholder_effect or '') 
                                + ' ' + (website.search_placeholder_direction or '') 
                                + ' ' + (website.search_placeholder_activation or '')"
                    t-att-style="'--anim-duration: %ss;' % (website.search_placeholder_duration or 2)">
                    <t t-esc="website.search_placeholder"/>
                </span>
            <div class="tp-search-wrapper">
        </xpath>
    </template>
</odoo>
